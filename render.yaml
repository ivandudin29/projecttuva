services:
  - type: web
    name: telegram-task-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py  # ← изменили здесь
    # ... остальное без изменений
